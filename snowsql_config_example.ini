# Example SnowSQL config (store this at %USERPROFILE%\.snowsql\config if you prefer profiles)
# NOTE: For security, prefer using environment variables (shown in the README) instead

[connections.demo_conn]
accountname = <account_identifier>
username = <username>
# Don't store plaintext passwords in repo/config. Use environment variables or OS secret stores.
# You can reference a profile by name from SnowSQL: `snowsql -c demo_conn -f "path/to/file.sql"`

[settings]
# default_output_format = json
